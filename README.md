# 📂 Estudos de PHP - Do Básico ao Processamento de Dados

Este repositório documenta minha jornada de aprendizado em **PHP**, partindo dos conceitos fundamentais de sintaxe até o desenvolvimento de lógica para processamento de dados e integração com formulários.

> 🎯 **Objetivo Profissional:** Estudante de **Análise e Desenvolvimento de Sistemas (ADS)** com foco em migrar para a área de TI, construindo uma base sólida em desenvolvimento back-end.

## 🚀 Evolução Técnica e Conceitos Explorados

Ao longo destes exercícios, foram aplicados os seguintes conhecimentos técnicos:

* **Sintaxe e Output:** Uso de `echo` para renderizar conteúdos dinâmicos e integração com tags HTML (`<h1>`, `<p>`, `<mark>`).
* **Comentários e Documentação:** Prática de boas práticas com comentários de linha (`//`, `#`) e de bloco (`/* */`).
* **Manipulação de Variáveis:** Declaração e exibição de tipos variados (Strings, Integers) e concatenação.
* **Estruturas de Controle e Lógica:**
  * Condicionais dinâmicas com `if`, `elseif` e `else`.
  * Estruturas de repetição `for` e `foreach`.
  * Controle de fluxo com o comando `break`.
* **Processamento de Dados:**
  * Manipulação de Arrays unidimensionais e multidimensionais (listas de funcionários).
  * Recebimento de dados via método `$_POST` para interação com formulários.
* **Estruturação de Dados com HTML:** Criação de tabelas com cabeçalhos horizontais (`<th>`) e verticais.
* 

## 📂 Organização dos Scripts

| Arquivo(s) | Foco do Estudo | Descrição Técnica |
| :--- | :--- | :--- |
| `1_Script` a `3_Script.php` | **Fundamentos** | Tags de abertura PHP, comandos de saída (`echo`) e tipos de comentários. |
| `4_Script` a `6_Script.php` | **Variáveis** | Declaração e exibição de variáveis de texto e numéricas. |
| `7_Script` a `10_Script.php` | **Formatação** | Estilização avançada via PHP usando tags como `<strong>`, `<b>`, `<i>`, `<u>` e `<mark>`. |
| `11_Script` e `12_Script.php` | **Strings Avançadas** | Interpolação de variáveis e uso de caracteres de escape em textos. |
| `13_Script` a `16_Script.php` | **Tipagem Dinâmica** | Verificação de variáveis vazias e inspeção de tipos de dados. |
| `17_Script` a `20_Script.php` | **Arrays e Índices** | Manipulação de vetores numéricos e dicionários (Arrays Associativos). |
| `21_Script.php` | **Foreach e Tabelas HTML** | Demonstração do uso de `foreach` para varrer arrays multidimensionais e renderização de dados em tabelas. |
| `22_Script.php` | **Arrays Multidimensionais** | Estruturação de dados complexos (times e pontuações) em arrays de múltiplas dimensões. |
| `23_Script.php` | **Iteração Simples** | Recapitulação do uso do `foreach` para percorrer valores de um array de strings. |
| `24_Script.php` | **Chave e Valor** | Uso do `foreach` para recuperar simultaneamente o índice (key) e o conteúdo (value) de um array. |
| `25_Script.php` | **Exemplo de Foreach** | Prática de iteração sobre um array contendo os dias da semana e exibição de seus índices. |
| `26_Script.php` | **Lógica com Arrays** | Aplicação de regras de negócio, como cálculo de bonificação salarial e verificação de status ativo/inativo dentro de um loop. |
| `27_Script.php` | **Situação de Veículos** | Organização e exibição de dados de estoque e vendas de veículos utilizando índices de matrizes. |
| `28_Script.php` | **Operador de Soma** | Execução de operações matemáticas básicas de adição entre variáveis numéricas. |
| `29_Script.php` | **Operador de Multiplicação** | Cálculo de produtos utilizando variáveis do tipo ponto flutuante e inteiros. |
| `30_Script.php` | **Operador de Divisão** | Demonstração de divisões sucessivas e exibição do resultado em tela. |
| `31_Script.php` | **Operador de Subtração** | Demonstração de cálculos combinando soma e subtração entre múltiplas variáveis. |
| `32_Script.php` | **Operador de Módulo (%)** | Cálculo do resto da divisão inteira entre duas variáveis. |
| `33_Script.php` | **Comparação de Tipos** | Teste de igualdade (`==`) entre números inteiros e strings, demonstrando a tipagem dinâmica do PHP. |
| `34_Script.php` | **Cálculo de Média** | Aplicação prática de operadores aritméticos e condicionais para determinar a aprovação de um aluno. |
| `35_Script.php` | **Incremento e Decremento** | Uso de operadores de pré-incremento (`++$n`) e pós-decremento (`$n--`) em expressões matemáticas. |
| `36_Script.php` | **Lógica AND e OR** | Uso dos operadores lógicos textuais `AND` e `OR` para validação de múltiplas expressões. |
| `37_Script.php` | **Operadores XOR e Negação** | Exploração do operador de exclusividade (`XOR`) e do operador de negação lógica (`!`). |
| `38_Script.php` | **Operadores `&&` e `||`** | Implementação de lógica booleana utilizando os operadores simbólicos padrão do mercado. |
| `39_Script.php` | **Manipulação de Strings** | Exemplos de concatenação simples e atribuição combinada (`.=`) para nomes. |
| `40_Script.php` | **Estrutura Condicional IF** | Implementação básica da estrutura de controle `if` para tomada de decisão simples. |
| `41_Script.php` | **Estrutura IF ELSE** | Implementação de decisão condicional composta para comparar dois valores. |
| `42_Script.php` | **Estrutura IF ELSEIF** | Utilização de múltiplas condições encadeadas para verificar maioridade, igualdade ou menoridade. |
| `43_Script.php` | **Condicionais Simples** | Exemplo de validação de valores numéricos específicos através de `elseif`. |
| `44_Script.php` | **Estrutura SWITCH** | Alternativa ao `if` para testar múltiplas ocorrências de uma variável numérica com `break`. |
| `45_Script.php` | **SWITCH com Strings** | Aplicação do `switch` para comparar cadeias de caracteres (nomes de frutas). |
| `46_Script.php` | **Loop WHILE (Pós-incremento)** | Execução de repetição enquanto a condição é verdadeira, usando `$n++`. |
| `47_Script.php` | **Loop WHILE (Pré-incremento)** | Demonstração da diferença de saída ao utilizar o incremento antes da exibição (`++$n`). |
| `48_Script.php` | **Sintaxe Alternativa WHILE** | Uso da sintaxe `while : endwhile`, comum em templates que misturam PHP e HTML. |
| `49_Script.php` | **Estrutura DO WHILE** | Garantia de que o bloco de código seja executado pelo menos uma vez antes da validação da condição. |
| `50_Script.php` | **Estrutura FOR** | Implementação de loop com contador determinado, ideal para iterações numéricas simples. |
| `51_Script.php` | **Sintaxe FOR ENDFOR** | Uso da sintaxe alternativa para o laço `for`, útil para organização de código em templates. |
| `52_Script.php` | **Iteração Numérica** | Exemplo prático de contagem e exibição de valores dinâmicos utilizando o laço `for`. |
| `53_Script.php` | **Interrupção com BREAK** | Demonstração de como forçar a saída de um loop infinito através de uma condição de parada específica. |
| `54_Script.php` | **Salto com CONTINUE** | Uso do comando `continue` para ignorar iterações específicas (números pares) e prosseguir no loop. |
| `55_Script.php` | **Desafio: Números Pares** | Lógica para apresentar números pares com uma condição de saída baseada em divisibilidade. |
| `56_Script.php` | **Desafio: Números Ímpares** | Implementação de filtro para exibir apenas números ímpares em um intervalo de 0 a 50. |
| `57_Script.php` | **Formulários: Método POST** | Criação de interface HTML para envio de dados simples (nome) para processamento em PHP. |
| `58_Script.php` | **Processamento de Form** | Script de recebimento e tratamento dos dados enviados pelo formulário do Script 57. |
| `59_Script.php` | **Entradas Múltiplas** | Formulário HTML puro com múltiplos campos de entrada para coleta de dados estruturados. |
| `60_Script.php` | **Recebimento de Campos** | Lógica de back-end para capturar e processar variáveis vindas de formulários complexos. |
| `61_Script.php` | **Formulário: Par ou Ímpar** | Interface HTML para entrada de um número a ser verificado. |
| `62_Script.php` | **Lógica: Par ou Ímpar** | Processamento back-end que utiliza o operador de módulo `%` para determinar a paridade. |
| `63_Script.php` | **Formulário: Cálculo de Bónus** | Interface para entrada de meses trabalhados, salário e bónus pretendido. |
| `64_Script.php` | **Lógica: Cálculo de Bónus** | Script para processar e exibir os cálculos financeiros enviados pelo formulário. |
| `65_Script.php` | **Formulário: Validação de Idade** | Interface de "Censura do Show" para recolha de nome e idade do cliente. |
| `66_Script.php` | **Lógica: Validação de Idade** | Script que processa a permissão de entrada com base na idade fornecida. |
| `67_Script.php` | **Tabelas HTML: Estrutura** | Exemplo básico de criação de tabelas com bordas, linhas e células. |
| `68_Script.php` | **Tabelas HTML: Cabeçalhos** | Uso de etiquetas `<th>` para definir títulos de colunas e melhorar a semântica. |
| `69_Script.php` | **Tabelas: Dados de Alunos** | Exibição organizada de informações (Nome, Cidade, Telefone) num formato de grelha. |
| `70_Script.php` | **Tabelas: Cabeçalhos Verticais** | Demonstração de layout de tabela com cabeçalhos dispostos verticalmente. |
| `receber_form.php` | **Saudação Dinâmica** | Script que captura o nome via POST e exibe uma mensagem de boas-vindas personalizada. |
| `prg_recebe_campos.php` | **Múltiplos Inputs** | Demonstração de captura e exibição de múltiplos campos de texto enviados simultaneamente. |
| `ExVerParImpar.php` | **Validação de Paridade** | Utiliza o operador de módulo `%` para identificar números e aplica estilização visual (Azul para Par, Vermelho para Ímpar). |
| `CalcBonus.php` | **Iteração Financeira** | Executa um cálculo acumulativo de salário e bônus utilizando um laço `for` para gerar um relatório mensal. |
| `Censura.php` | **Controle de Acesso** | Aplica regras de negócio baseadas na idade para determinar níveis de permissão em eventos, usando cores para status. |


## 🛠️ Tecnologias Utilizadas

* **PHP** (Lógica de programação e back-end)
* **HTML5** (Estruturação de interfaces e formulários)

---
**Sinki** | *Estudante de Análise e Desenvolvimento de Sistemas*
